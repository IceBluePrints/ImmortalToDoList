import React from 'react';
import { LogoWrapper, Title } from './Logo.style';

const Logo = () => {
  return (
    <LogoWrapper>
      <Title>To do list</Title>
    </LogoWrapper>
  );
};

export default Logo;
